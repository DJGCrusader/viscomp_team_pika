%%
% getConfidence is  method that takes an array of images names and a list of the classes. It returns a confidence matrix of how 
% confident an image is classified in each class.  The the confidence matrix is of size length(image names) X length(classes)
%%